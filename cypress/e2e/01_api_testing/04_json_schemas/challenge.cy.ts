// challenge: go to https://www.liquid-technologies.com/online-json-to-schema-converter and create a schema for lists and cards response bodies. add them to the test and test the responses of given endpoints against them
it('GET /api/lists', () => {
  
});

it('GET /api/cards', () => {
  
});
